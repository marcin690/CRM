package wh.plus.crm.model.order;

/** Zamówienie pierwotne vs domówienie (uzupełnienie do istniejącego zamówienia). */
public enum OrderKind {
    ORDER,    // zamówienie
    REORDER   // domówienie
}
