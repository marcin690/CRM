# Lokalna baza danych
spring.datasource.url=jdbc:mysql://localhost:3306/crm_dev?serverTimezone=Europe/Warsaw
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Schemat bazy automatycznie tworzony przy starcie
spring.jpa.hibernate.ddl-auto=update

# Port dla lokalnego ?rodowiska
server.port=8080

# Logi dla deweloperów
logging.level.org.springframework=DEBUG
logging.level.org.hibernate=DEBUG

# Wy?wietlanie zapyta? SQL w logach
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
